[build]
  base = "client"
  command = "npm run build"
  publish = "dist"

# React Router: alle stier → index.html (unngår 404 etter Google-innlogging)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
