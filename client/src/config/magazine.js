/**
 * Merkevare og rute for den redaksjonelle siden.
 * Endre navn her én gang — oppdateres overalt det importeres.
 */

import { BRAND_NAME } from "./brand";

/** Kort navn i meny og topp — bredt: ikke bare jobb, men perspektiver og nyheter */
export const MAGAZINE_NAME = "Utblikk";

/** Undertittel under logoen på siden */
export const MAGAZINE_TAGLINE = `Nyheter, meninger og innsikt — fra ${BRAND_NAME}, fra bedrifter, og om det som rører seg i samfunnet`;

/** URL-sti (liten bokstav, ingen mellomrom) */
export const MAGAZINE_PATH = '/utblikk';
